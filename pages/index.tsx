import type {NextPage} from 'next'
import Hero from "../components/Hero";

const Home: NextPage = () => {
    return (
        <header>
            <Hero/>
        </header>
    )
}

export default Home
